import { en } from './en';
import { Translations } from './types';

// Currently defaults to English. 
// intended to be extended for multi-language support in the future.
export const t: Translations = en;

export default t;
